/**
 * PromptLayer 自动化工具配置文件
 * 如果不使用自定义配置，可删除此文件或直接使用默认值
 */

module.exports = {
  // Chrome浏览器可执行文件路径（留空使用系统默认）
  browserPath: '',
  
  // 是否使用无头模式 (true/false)
  headless: false,
  
  // 操作延迟（毫秒），调试时可增大此值
  slowMo: 0,
  
  // 注册账号使用的邮箱域名列表
  emailDomains: [
    '@vkgl.keomiao.space',
    '@coat.suitbase.cfd',
    '@gost.suitbase.cfd',
    '@ltd.keomiao.space'
  ]
};
